version: '3'

networks:
  nginx_proxy_network:
    external:
      name: nginx_proxy_network

services:
  app:
    build: .
    restart: "always"
    networks:
      - nginx_proxy_network
    environment:
      VIRTUAL_HOST: covidbezockovani.cz
      VIRTUAL_HOST_ALIAS: www.covidbezockovani.cz
      LETSENCRYPT_HOST: covidbezockovani.cz,www.covidbezockovani.cz
    volumes:
      - $PWD:/var/www/html:cached